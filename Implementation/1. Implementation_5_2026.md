
7. full report that can be downloaded in pdf or excel 
    - refromat the pdf file to be more interactive and nice 
    - export in excel is not working

-----------------------New-----------------------
1. close of exit fees should be visible in the total fees in trades details 
    - the fees value should be visible bside the equation of fees
2. Transaction History doesn't show the dividends, the cash review 
3. dividends should be added in trades journal 
    - when a dividend is chosen to be added the available trades pops up to choose which onw to add then add a value or a stock dividends for it 
    - then adjust the no of stocks, avg value of the stocks after adding the dividend

3. when an exit price is given for a trade this means that the trade is closed so dont prevent the adding of this trade if the availave cash is not sufficient
4. symbol in adding trades should be capitalized always for example for tesla it should be TSLA
5. Trade type in trades journal should have trade
6. Trade details & actions the exit fees should be also calculated automatically 

7. strategy type in trades journal should be like tags 
    - by clicking it shows trades with the same color as the tag 
    - can be searched for trades that have the same tag or combined tags in a separated page like symbols page
    - for the tags we can select more than one 
    - if i clicked the box of the stratgy a list poops up with available stratgies and filtered as i type letters, if it doesn't exist the list provide a "+" sign to indicate the ability to add it.
    - database should have tags in it for each trade
    - settings should have a section with the abiltiy to remove the wrong tags added by wrong ro modify it

8. the app should suggest the stock name when addding trades and know that name is a fund or stock etc also the default strategies with each trade 
----------------------------


Then
11. how to make it exe that can run from start after installation 
    - the app should be installed on the device when download the exe file from the website 
    - add a feature so that the app can be updated with this sources code changes in the future (what you will do or how it will be done)
    - the app should use an offline database for the app's data
    - create a workflow to conver the app into exe as needed 